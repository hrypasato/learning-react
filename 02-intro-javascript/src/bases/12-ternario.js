const activo = true;

/* 
let mensage = '';
if(activo){
    mensage = 'Activo';
}else{
    mensage = 'Inactivo';
} */

//if else
//const mensage = (activo) ? 'Activo' : 'Inactivo';

//only if
const mensage = activo && 'Activo';

console.log(mensage);